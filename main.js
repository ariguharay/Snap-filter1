function preload() {
}

function setup() {
    canvas = createCanvas(350, 300);
    canvas.position(480,250);
}

function draw() {
}

function take_snapshot(){
    save('myFilterImage.png')
}